# Copy to `.env` and fill in before running `npm run release`.
# .env is gitignored; this template is not.
#
# GitHub fine-grained PAT with Contents: Read and write on the release repo.
# Generate at https://github.com/settings/personal-access-tokens/new
GH_TOKEN=
